class GHAGTomeOfPower : ArtiTomeOfPower replaces ArtiTomeOfPower
{
	override bool use(bool pickup)
	{
		bool result = Super.Use(pickup);
		if (result)
		{
			Gardevoir(owner).RageValue = Gardevoir(owner).RageConstant;
			Gardevoir(owner).getLivid();
		}
		return result;
	}
}